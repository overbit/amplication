import { Decimal } from "decimal.js";

export type Systemenv = {
  appbaseprice: Decimal;
  coorduserId: number;
  domainId: number;
  expdate: Date;
  expdate2: Date;
  id: number;
  sysemail: string;
};
